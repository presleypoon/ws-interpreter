# This is my project to make a HTML interpretator that instead of supporting JS, support Rust

In theory, this is take ages, so if you see this, be patient, give advice if you know Rust or report any bugs, give the error(if any) or if screenshot if necessary

## How to use this

Add everything you want to interpret into `lib\src`, `main\src\main.rs` is what I'm going to code
